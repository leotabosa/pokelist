import ErrorMessage from '@/components/ErrorMessage.vue'

describe('ErrorMessage.vue', () => {
  it('ErrorMessage - SHOULD be a valid Vue component', () => {
    expect(ErrorMessage).toBeAValidComponent()
  })
})
